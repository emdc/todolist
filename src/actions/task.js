export const ACTIONS = {
  ADD: 'TASK_ADD',
  REMOVE: 'TASK_REMOVE',
  RENAME: 'TASK_RENAME',
  COMPLETE: 'TASK_COMPLETE'
};

export const add = (task) => (dispatch, getState) => {

};

export const remove = (taskId) => (dispatch, getState) => {

};

export const rename = (taskId, newTitle) => (dispatch, getState) => {

};

export const complete = (taskId) => (dispatch, getState) => {

};
